# Live proxy evidence

Actual client -> local Routetok /v1/chat/completions (jev-auto) -> live Jev -> OpenRouter generation. No mocked inference. Test policy: explicit-text-v2, confidence floor .8, writing GLM5.3Flash, coding Qwen3.8Flash, reasoning DeepSeek4.1Flash, explicit uncertainty fallback DeepSeek4.1Flash. Three requests, max_tokens 2048, no generation retries. This policy is an experimental test configuration, not auto-promoted production quality evidence.

Exact checks all passed: email mira@example.org via GLM (1790 ms); Python aliasing [[2,3],[9,2],[2,3]] via DeepSeek (5424 ms); logical answer no via DeepSeek (8344 ms). Classifier confidences 1, .5, 1 respectively. Coding request therefore took explicit fallback instead of returning previous routing-uncertain failure. Qwen was configured but NOT dispatched in this run. This demonstrates functioning selection/fallback/transport, not optimal selection or new-task generalization. Known development examples only.

Reported OpenRouter generation cost $.00036888525; Jev 1888 tokens, dollar cost unknown. Three exact assertions passed; build and full test suite passed. Test server stopped, private temporary key file deleted. Raw synthetic responses and usage included, no credentials. Added classifier confidence, missing-information probability and uncertainty-fallback reason to observer payload, propagated by proxy into recent metrics events. This fixes the prior inability to distinguish a deliberate uncertainty fallback from a normal selection in those traces. Confidence remains classification confidence, never predicted answer quality.
