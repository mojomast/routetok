# Explicit-contract development pilot

Correct user-authorized key used; prior HTTP403 run used a different environment key. Nine real generation attempts, eight task-policy trials (two tasks x two repeats x two models). No Jev calls. Mercury8192 passed4/4 on first attempt, cost$0.00128690. Qwen16384 passed3/4; remaining trial exhausted limit on both initial and permitted repair, total model cost$0.02356002. Combined provider-reported cost$0.02484692, no missing bills.

Application checks alone triggered repair; separate scoring checks never informed a retry. Reference, empty-output and weak-validator regression checks passed before inference. Public response retention intentional. Raw journal and immutable manifest included. Both cases were previously inspected development cases with deliberately clarified contracts, NOT independent holdout or a controlled causal comparison to earlier runs. No claim of general superiority or proven repair effectiveness.

Timing caveat: urllib timeout is a socket timeout, NOT a hard total wall deadline. Two Qwen requests exceeded240 seconds while completing; observed303420ms and307548ms. A hard wall deadline remains an infrastructure improvement before scaling. No production defaults changed.
