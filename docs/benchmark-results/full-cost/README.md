# Full-cost real proxy comparison

Two runs of 48 actual requests each, four policies x existing 12 synthetic development tasks. No replay: client HTTP runner -> Routetok -> optional real Jev -> actual provider generation. 8192 output allowance, bounded 4 concurrency, frozen binary routine Qwen / demanding DeepSeek (.8 confidence). No automatic paid retries. Same cases previously inspected; NOT untouched heldout, NOT source-separated published benchmark. Four template clusters, not 12 independent task families.

New request-correlated x-routetok-jev-input-tokens/output-tokens headers let evaluator join classification usage to generation charge on the exact response. Rates explicitly configured $.042/M input, free output; unknown usage yields null, not zero. Per-row generationCostUsd and classifierEstimateUsd retained. Full totals are reported generation charges PLUS public-list-price classifier estimates, not invoice proof. Existing dashboard now explicitly labels this basis. These headers contain usage only, no raw prompts or credentials.

20-second first-event default run: Mercury12/12, Jev12/12, Qwen11/12, DeepSeek10/12. Three HTTP504s with missing billing. Full run retained rather than discarding errors. Increased firstEventTimeoutMs to120000 in isolated test config and reran all policies (not only failures). Caller deadline180s. Long-deadline run all policies12/12: Mercury $.00228872 p50 1202ms; Qwen $.002063136 p50 2716ms; DeepSeek $.005984285 p50 2102ms; Jev $.003975514 p50 3227ms. Jev total includes $.000468468 list-price classification for11154 input tokens and636 free output tokens. Jev33.57% lower than DeepSeek but92.69% MORE than Qwen. Thus saves vs flagship on this development batch, does not beat best fixed cheap baseline. No proof of population equal quality. Different draws and provider variability prevent attributing all cross-run changes to deadline alone.

Headless Chromium Playwright exercised actual dashboard twice, asserted panel contents and zero page JavaScript errors, and produced screenshot /tmp/routetok-full-cost-dashboard.png. Browser service tool failed HTTP500; local Playwright alternative succeeded. No production activation. Servers stopped. Typecheck/build/full suite and HTTP cost-join regression pass. Raw journals include started rows then completed rows; analyze latest per key. Historical successful rows have failure='answer' despite pass=true; new runner fixes this label, pass is authoritative in archived rows.

Resume hash currently covers request config and pricing, not mutable server policy/config: freeze and archive these separately; never resume after modifying server policy. Two runs stored separately. Remaining product gates: genuinely source-disjoint benchmark, calibration/heldout power and cluster-aware intervals, invoice verification, safe production validation fallback.
