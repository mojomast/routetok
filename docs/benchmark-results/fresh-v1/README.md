# Fresh synthetic validation

60 live attempts: five models x 12 fresh cases, four workers, no retries, 2048 output cap. Four template clusters (ledger idempotency, exhaustive optimization, 90-record version lookup, Python aliasing), three parameter variants each. These are new prompts frozen before requests, NOT a representative source-independent holdout; effective template diversity is four, not twelve independent task families. This does not complete the proposed production benchmark.

Mercury 11/12 $.00206854 median 1527.5ms; Qwen3.8 11/12 $.00334 median 4486.5ms; DeepSeek4.1 10/12 with two 429 responses, incomplete known cost $.0024558; Ling 10/12 $.000583065; GPT-OSS20B 9/12 with three 429 responses, incomplete known cost $.00055182. Known experiment charges $.008999225. Missing bills are unknown. Mercury's observed cost 38.07% below Qwen at same aggregate strict score, NOT established quality noninferiority. Different failures: Mercury exhausted output on optimization; Qwen returned correct contents inside unexpected object on code. Ling had fenced correct code-state output and optimization truncation. All actual DeepSeek/GPT-OSS completed responses passed.

Run contains no new Jev calls. No model default promoted. Do not compare incomplete aggregate DeepSeek cost as if complete. Need randomized/interleaved dispatch and paced availability follow-up; model-major job order can confound providers/time. Synthetic validators must not be supplied to runtime as hidden-answer oracles. Optimization truth computed by exhaustive subset enumeration.

Added buffered application-level validatedFallback helper with eligible IDs, explicit upper-bound reservations, two-attempt limit, unknown-cost stop and model-substitution check. Caller owns accurate conservative reservations and application-specific validation. It is NOT wired into proxy streaming or production dispatch. Transport errors propagate rather than automatic paid retry. Tests exercise actual helper for bounded success, budget refusal and unknown billing. JSON syntax alone cannot validate semantics. Typecheck and full suite pass. Dashboard integration, source-separated representative heldout corpus and full policy comparison remain outstanding.
