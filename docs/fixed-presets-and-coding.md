# Opt-in fixed-model presets and coding evaluation

presets/qwen-low-cost.json and presets/mercury-low-latency.json are operator-selected config templates, not auto-applied defaults. For a NEW isolated DATA_DIR copy the chosen JSON to config.json, supply OPENROUTER_API_KEY securely, launch Routetok and request the exact enabledExternalModels ID via /v1/chat/completions. For an existing installation merge reviewed fields rather than overwrite credentials/settings. Names describe candidate positioning, not a universal optimum. Single attempt, explicit-model fallbacks disabled, no paid fallback chain; no Jev authorization. Existing defaults unchanged. Prior proxy smoke exercised both exact IDs; new preset regression checks enforce these constraints.

Coding pilot freezes MBPP partitions before inference (commit284efd5): four development, eight heldout public tasks. First provided assertion supplies interface example; all three dataset assertions executed. Reference solutions verified in the same sandbox before generation. Four models x12 tasks =48 real OpenRouter attempts, three concurrent, 8192 output allowance, no retries and provider fallback disabled. This script calls OpenRouter directly, not proxy routing. Source and license retained; public benchmark contamination possible. Generation-only single-function tasks, NOT repository repair or tool-agent evaluation.

Docker pinned image digest, network none, read-only root, no host mounts, no host environment or keys, unprivileged uid, caps dropped, no-new-privileges, 128MiB memory, half CPU,16 pids, fd/file-size limits, ephemeral8MiB tmpfs, wall timeout with container removal. Output discarded to avoid log flooding. Self-tests actually checked working code, assertion failure, outbound network denial, root filesystem write denial, infinite-loop timeout. Docker shares host kernel: defense in depth, not VM-grade isolation; use a dedicated worker for untrusted public service. Assertions run in candidate interpreter, so not tamper-proof against malicious test bypass. Do not use as security guarantee.

Heldout Mercury7/8 $.00141732 median1886.5ms; QwenFlash6/8 $.01171943 median41671ms; DeepSeek5/8 (six completed, two429) known$.0024546; QwenCoder0/8 $.00026468. Known combined development/heldout charge$.01878497; missing bills remain unknown. QwenFlash two length-limited failures. Mercury one executable test failure. All reference solutions passed. No markdown stripping or repair. Response text explicitly retained ONLY for public coding corpus and archived for reproducibility. Failures can include empty code, format, runtime and semantics; do not equate every strict failure with lack of coding ability.

General HTTP runner now records finish reason and usage. Response retention requires BOTH publicBenchmark:true and retainPublicResponses:true, rejects invalid request before dispatch; default retains no text. Reports still whitelist aggregates. New tests cover preset restrictions and retention denial. Classifier experiments remain gated: summarize-coding.py lists observed successes over other COMPLETED failures but explicitly blocks promotion on this single-draw tiny pilot. No new Jev policy or paid classifier calls: no demonstrated reproducible specialist complementarity yet. This implements the conditional decision to NOT add another classifier when evidence does not support it.

Remaining limitations: source-overlapping row-disjoint public split, small task count, provider rate-limit confounding, no repeated generations, no repository repair. Heldout now inspected; do not reuse as untouched proof. Full TypeScript checks/tests and real sandbox checks passed. Raw coding artifact retention is intentionally opt-in; no private user prompts included.
