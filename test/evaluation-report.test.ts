import test from 'node:test';import assert from 'node:assert/strict';import {writeFileSync,mkdtempSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {evaluationSummary} from '../src/evaluation-report.js';
test('dashboard exports aggregate whitelist, never arbitrary report secrets',()=>{const dir=mkdtempSync(join(tmpdir(),'report-'));try{const f=join(dir,'report');writeFileSync(f,JSON.stringify({kind:'measured_http_task_evaluation',identity:'fixture',secret:'hidden',models:[{model:'a',attempts:1,passes:1,totalCostUsd:null,secret:'hidden',prompt:'hidden'}]}));const out=JSON.stringify(evaluationSummary(f));assert(!out.includes('hidden'));assert(out.includes('totalCostUsd'));assert.deepEqual(evaluationSummary(),{available:false});}finally{rmSync(dir,{recursive:true});}});
