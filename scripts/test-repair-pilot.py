import runpy
ns=runpy.run_path('scripts/repair-pilot.py');refs=[{'state.py':'def fresh(): return {}','ledger.py':'def apply(events):\n b={};seen=set()\n for i,a,d in events:\n  if i not in seen: seen.add(i);b[a]=b.get(a,0)+d\n return b'}, {'codec.py':'from urllib.parse import parse_qsl\ndef pairs(s): return parse_qsl(s,keep_blank_values=True)','api.py':'from codec import pairs\ndef parse(q):\n out={}\n for k,v in pairs(q): out.setdefault(k,[]).append(v)\n return out'}, {'clockutil.py':'def expired(now,deadline): return now>=deadline','cache.py':'from clockutil import expired\nclass Cache:\n def __init__(self,clock): self.clock=clock;self.data={}\n def set(self,k,v,ttl): self.data[k]=(v,self.clock()+ttl)\n def get(self,k):\n  if k not in self.data:return None\n  v,end=self.data[k]\n  return None if expired(self.clock(),end) else v'}, {'checks.py':'def valid(a,b):return a<=b','api.py':'def merge(items):\n out=[]\n for a,b in sorted(items):\n  if a>b:raise ValueError()\n  if a==b:continue\n  if out and a<=out[-1][1]:out[-1]=(out[-1][0],max(b,out[-1][1]))\n  else:out.append((a,b))\n return out'}]
for t,r in zip(ns['TASKS'],refs):
 assert ns['validate'](r,t['visible']);assert ns['validate'](r,t['hidden']);print(t['id'],'reference passed')
