import {readFileSync,statSync} from 'node:fs';
export function evaluationSummary(file?:string){if(!file)return {available:false};if(statSync(file).size>1048576)throw Error('report_too_large');const r=JSON.parse(readFileSync(file,'utf8'));if(r.kind!=='measured_http_task_evaluation'||!Array.isArray(r.models)||r.models.length>100)throw Error('invalid_report');return {available:true,kind:r.kind,identity:String(r.identity).slice(0,64),models:r.models.map((m:any)=>{const out:any={model:String(m.model).slice(0,200)};for(const key of ['attempts','passes','missingBills','knownCostUsd','totalCostUsd','costPerSuccessUsd','p50Ms','p95Ms'])out[key]=typeof m[key]==='number'&&Number.isFinite(m[key])&&m[key]>=0?m[key]:null;return out;})};}
